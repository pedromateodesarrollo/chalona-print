# Vhost nginx para print.chalonasoft.com → hub de chalona-print (127.0.0.1:3070).
#
# Instalar:
#   sudo cp nginx-print.chalonasoft.com.conf /etc/nginx/sites-available/print.chalonasoft.com
#   sudo ln -s /etc/nginx/sites-available/print.chalonasoft.com /etc/nginx/sites-enabled/
#   sudo certbot --nginx -d print.chalonasoft.com
#   sudo nginx -t && sudo systemctl reload nginx
#
# Requisito previo: DNS A de print.chalonasoft.com → IP del box.

map $http_upgrade $connection_upgrade_print {
    default upgrade;
    ''      close;
}

server {
    server_name print.chalonasoft.com;

    location / {
        proxy_pass http://127.0.0.1:3070;
        proxy_http_version 1.1;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        $connection_upgrade_print;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Un trabajo puede ser un PDF; el tope real lo pone PRINT_MAX_TRABAJO_MB
        # en el servicio, esto solo evita que nginx corte antes.
        client_max_body_size 32m;

        # Los agentes viven colgados de un WebSocket durante días. Sin esto,
        # nginx los corta cada minuto y reconectan sin parar.
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    listen 80;
    listen [::]:80;
}
